# assets/

Static images, icons and fonts imported directly into components (bundled
and hashed by Vite). Files here are for framework/branding assets used by
the app shell — page-specific illustrative content should live next to the
page that uses it if it isn't reused elsewhere.
