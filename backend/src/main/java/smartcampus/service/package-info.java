/**
 * Business logic — the service layer.
 *
 * <p>Services own every rule in the system: validation beyond field constraints,
 * authorization decisions, transaction boundaries, calculations such as attendance
 * percentage and GPA, and orchestration of repositories and external providers (AI,
 * code execution, mail). Controllers stay thin because this layer is where the
 * behaviour lives.
 */
package smartcampus.service;
