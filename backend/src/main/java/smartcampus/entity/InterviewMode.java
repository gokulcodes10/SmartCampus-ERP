package smartcampus.entity;

/**
 * The mode of an interview.
 *
 * <p>Stored as the string name (not the ordinal) in {@code interviews.mode}.
 */
public enum InterviewMode {
    ONLINE,
    ONSITE,
    PHONE
}
