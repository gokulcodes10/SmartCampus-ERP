package smartcampus.entity;

/**
 * The difficulty level of an interview question.
 *
 * <p>Stored as the string name (not the ordinal) in
 * {@code interview_questions.difficulty}.
 */
public enum InterviewDifficulty {
    EASY,
    MEDIUM,
    HARD
}
